# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## โปรเจกต์นี้คืออะไร

**MORE PRINT** — เว็บรับปริ้นเอกสารพร้อมส่ง 24 ชม. รอบมหาวิทยาลัยขอนแก่น (มข.)

- เว็บลูกค้า: https://more-print.github.io/more-print-app/
- หลังบ้านเจ้าของร้าน: https://more-print.github.io/more-print-app/admin.html
- GitHub repo: `more-print/more-print-app` (public)

⚠️ **ระบบนี้เปิดใช้งานจริง มีลูกค้าจริงสั่งออเดอร์อยู่** — ตาราง `orders` ใน Supabase คือข้อมูล production ห้ามทดสอบทิ้งขยะโดยไม่เก็บกวาด (สร้างออเดอร์ทดสอบแล้วต้องแจ้งเจ้าของให้ลบ เพราะ anon ลบเองไม่ได้)

## สถาปัตยกรรม

**Static site แบบไม่มี build step** — แก้ไฟล์ → push → ออนไลน์ ไม่มี npm/bundler/test framework

| ไฟล์ | คืออะไร |
|---|---|
| `index.html` | แอปลูกค้าทั้งแอปในไฟล์เดียว (React 18 UMD + Babel-in-browser, class component `App` เดียว) |
| `admin.html` | หลังบ้านทั้งแอปในไฟล์เดียว (function components + hooks) — แท็บ 👥 ลูกค้า = วิเคราะห์ลูกค้าประจำ **เฉพาะงาน Standard** (Pro แยกไปอยู่ commission/pro): จัดกลุ่มตามเบอร์ ไม่นับที่ยกเลิก, ป้าย ⭐ประจำ(≥5)/🔁ซ้ำ(2-4)/🆕, ชิปงานที่สั่งบ่อยสุด 2 อันดับ (`topJobs`), เรียงบ่อย/ยอด/ล่าสุด + ค้นหา — เกณฑ์ป้ายนี้ใช้เหมือนกันทั้ง 3 หลังบ้าน; commission.html มีกล่อง 👥 ของ Pro (เห็นเบอร์), pro.html มีลิสต์เดียวกันให้พาร์ทเนอร์ (ชื่อ+สถิติ **ไม่มีเบอร์** — ห้ามใส่เบอร์กลับ) |
| `commission.html` | เว็บดูค่าคอมของเจ้าของ (login is_admin เท่านั้น, storageKey 'mp-comm'): รับรู้คอมเมื่องานส่งเสร็จ (status 4) — การ์ดคอมวัน/เดือน/ปี + คอมรอรับ + ตารางรายเดือน + ไทม์ไลน์ทีละงาน (ชำระ→ผลิต→ถึงมือ→โอนส่วนแบ่ง); `COMMISSION` ต้องแก้ให้ตรงกับ pro.html เสมอ |
| `pro.html` | หลังบ้านงาน PRO สองบทบาท (storageKey 'mp-pro'): **เจ้าของ** (`is_admin`) = ตั้งราคางาน (update print_cost+total), กดบันทึกการโอน (`orders.payout_at`), เห็นลูกค้า/เบอร์ · **ร้านพาร์ทเนอร์** (`is_partner` + ตาราง `partners`, RLS ให้ SELECT เฉพาะ ptype='pro') = ดูงาน+ไฟล์ที่ต้องปริ้น+ส่วนแบ่ง 80% อย่างเดียว ไม่เห็นเบอร์ลูกค้า/รายชื่อลูกค้า/งาน standard; สมัครบัญชีได้จากหน้า login แล้วเจ้าของ INSERT uid เข้าตาราง partners ทาง SQL; คอม `COMMISSION=0.20` (ดีลสุดท้าย 2026-08-20: เรา 20/ร้าน 80) แก้ที่ const เดียว |
| `config.js` | ค่าคอนฟิกรันไทม์: Supabase URL/key, LIFF ID, LINE OA — โหลดด้วย `<script>` เป็น `window.MP_CONFIG` |
| `.github/workflows/keepalive.yml` | cron จ.+พฤ. ping ฐานข้อมูล กัน Supabase free tier พักโปรเจกต์ |

บริการภายนอก:
- **Supabase** (project ref `plfghesoscfqdlthkzwq`): Postgres (`orders`, `banners`, `admins`, `reviews`) + Storage bucket `uploads` (public) + Auth + Edge Function `smart-handler` (แกะลิงก์สั้น Google Maps → URL ปลายทาง)
- **GitHub Pages**: โฮสต์ (legacy build จาก main root)
- **Leaflet + OpenStreetMap**: แผนที่ปักหมุดจุดส่ง; **Photon** (photon.komoot.io) ค้นหาสถานที่ (bias ใกล้ มข.); **Nominatim** reverse-geocode/fallback
- **LINE**: OA `@346kifvy` + LINE Login ผ่าน LIFF ID `2010897028-o3azGJrL` (channel 2010897028, region Thailand)
- **pdf.js** (โหลด lazy): นับจำนวนหน้า PDF ในเครื่องลูกค้า

## คำสั่งที่ใช้บ่อย

```bash
# ดูเว็บในเครื่อง
python3 -m http.server 8791   # แล้วเปิด http://localhost:8791

# deploy = push อย่างเดียว (Pages build เอง ~1-2 นาที)
git add . && git commit -m "..." && git push

# เช็คว่า build เสร็จหรือยัง
gh api repos/more-print/more-print-app/pages/builds/latest --jq '.status'
```

- **CDN cache ของ Pages ค้าง ~10 นาที** — เช็คของใหม่ให้ใส่ query `?cb=อะไรก็ได้`
- ทดสอบหลังบ้านโดยไม่แตะข้อมูลจริง: copy `admin.html` + `config.js` ที่มีค่าว่าง ไปโฟลเดอร์ชั่วคราวแล้ว serve → เข้าโหมด demo (ข้อมูลตัวอย่าง, ไม่ต้องล็อกอิน)
- ตรวจข้อมูลจาก REST โดยตรง: header `apikey: <SUPABASE_ANON_KEY จาก config.js>` (อ่าน orders ไม่ได้ — ดู Security ด้านล่าง)

## โมเดลความปลอดภัย (สำคัญที่สุด — พังง่ายถ้าไม่เข้าใจ)

- **anon (ลูกค้า) = ฝากออเดอร์อย่างเดียว**: INSERT `orders`, INSERT storage, SELECT `banners` เท่านั้น — SELECT/UPDATE/DELETE orders ถูกปิดหมด
- ลูกค้าติดตามสถานะผ่าน RPC `get_order_status(oid)` (SECURITY DEFINER, ต้องรู้รหัสออเดอร์เป๊ะ) — รหัสออเดอร์เป็นสุ่มยาว `#MP-XXXXXXXX` จึงเดาไม่ได้
- **หลังบ้าน = Supabase Auth จริง**: ตาราง `admins` + ฟังก์ชัน `is_admin()`/`claim_admin()` (คนแรกที่เคลมได้สิทธิ์ — เจ้าของเคลมไปแล้ว) ไม่มี PIN แล้ว (`ADMIN_PIN` ใน config.js เป็นซากเก่า)
- **สองไคลเอนต์ใน index.html**: `this.db` (persistSession:false → เป็น anon เสมอ ใช้กับข้อมูล/ไฟล์) แยกจาก `this.auth` (ล็อกอินสมาชิก) — **ห้ามรวมกัน** ไม่งั้นสิทธิ์ RLS เพี้ยนทั้งระบบ
- 🔥 **ห้ามใส่ `upsert: true` ในการอัปโหลดฝั่งลูกค้าเด็ดขาด** — upsert ต้องใช้สิทธิ์ UPDATE ซึ่ง anon ไม่มี จะเจอ "new row violates row-level security" ทั้งที่ insert เฉย ๆ ผ่าน (เคยเป็นบั๊กจริง ไฟล์ลูกค้าหายทั้งวัน)
- ไฟล์ใน bucket public เข้าถึงได้ถ้ารู้ URL เป๊ะ (path มีรหัสออเดอร์สุ่ม = เดาไม่ได้) แต่ **ลิสต์รายการไฟล์ไม่ได้**

## วงจรออเดอร์

- `status`: `0` = ยกเลิก (มี `cancel_reason`), `1→4` = รับออเดอร์ → กำลังปริ้น → กำลังจัดส่ง → ส่งแล้ว; `times` = jsonb เวลาแต่ละขั้น
- **ออเดอร์ยกเลิกลบตัวเองใน 2 ชั่วโมง** (2026-08-19 นโยบายเจ้าของ — เดิม 15 นาที แล้วขยายเป็น 2 ชม.): trigger `trg_set_cancelled_at` ตีเวลา `orders.cancelled_at` เมื่อ status→0; หลังบ้านทั้ง 3 ซ่อนทันที + ลบถาวร (ไฟล์+สลิป+แถว, แชทลบตาม trigger) เมื่อเกิน 2 ชม. — ระวัง: สลิปหายด้วย เรื่องคืนเงินต้องเคลียร์ในช่วงนั้น; หน้าบ้านเก็บ `mp_order.cancelledAt` แล้วเก็บกวาดจอตัวเองครบ 2 ชม. (ไม่มี cancelledAt = รุ่นเก่า ลบทันที)
- **ลูกค้ายกเลิกเองได้** (2026-08-16): ปุ่มบนหน้าติดตาม เฉพาะสถานะ 1 + ไม่เกิน `CONFIG.cancelWindowMin` (10) นาทีหลังชำระ (นับจาก `mp_order.ts`) — ตัดสินจริงที่ RPC `customer_cancel(oid)` (SECURITY DEFINER: เช็ค status=1 + อายุ ≤10 นาที ฝั่งฐานข้อมูล **เลข 10 ใน SQL ต้องแก้คู่กับ CONFIG**) แล้วตั้ง `cancel_reason` ขึ้นต้น "ลูกค้ายกเลิกเอง" → หน้าเว็บใช้ prefix นี้แยกข้อความ "คุณยกเลิกเอง" จาก "ร้านยกเลิก" และ EF ใช้ยืนยันก่อนแจ้ง LINE เจ้าของ (`notify-order?action=customer-cancelled` +พาร์ทเนอร์ถ้างาน Pro) — ห้ามใช้ prefix นี้กับการยกเลิกจากหลังบ้าน
- ฝั่งลูกค้าจำ session ด้วย localStorage: `mp_user` (โปรไฟล์) และ `mp_order` (ออเดอร์**ที่เลือกดู** — โค้ดติดตาม/แชท/ยกเลิกผูกกับตัวนี้) และ `mp_orders` (2026-08-19 **ลิสต์ทุกออเดอร์ที่สั่งไล่เลี่ยกัน** สูงสุด 10 — ทุกจุดที่เขียน mp_order ต้อง upsertOrderList ด้วย; refreshOrderList โพลใบอื่นทุก 30 วิ เสร็จ→ประวัติ) และ `mp_history` (ประวัติออเดอร์ที่สำเร็จแล้ว สูงสุด 30 — แท็บ "ประวัติ" กลางแถบล่าง เก็บอัตโนมัติเมื่อสถานะถึง 4 ทั้ง standard/pro, แชทต่อออเดอร์เปิดจากประวัติได้ผ่าน state `chatFor`; ประวัติอยู่ในเครื่องลูกค้าเท่านั้นเพราะ anon SELECT orders ไม่ได้)
- **ระดับงาน 2 แบบ** (state `tier`): `standard` = ปริ้นขาวดำ + ปริ้นสี A4 เท่านั้น (ป้าย 'สีเลเซอร์'→'เอกสารสี'→'ปริ้นสี' และ 'เอกสารขาวดำ'→'ปริ้นขาวดำ' 2026-08-18 แก้ทั้ง PRINT_TYPES ใน index.html และ PTYPE_LABELS+fallback ใน admin.html; **ค่า ptype ในฐานข้อมูลยังเป็น `bw`/`color` เหมือนเดิม** เปลี่ยนแค่ป้ายที่แสดง) · การเข้าเล่มเหลือ ไม่เข้าเล่ม/เย็บมุม ฟรี (เดิม ฿5 — แก้ 2026-08-24)/เข้าเล่ม ฿40 (งดรับ 22:00-08:00 ปุ่มปิด+บล็อกที่ toPay — ทำได้เฉพาะช่วงเช้า นโยบาย 2026-08-24) — `landscape` (เล่มแนวนอน ฿60) ตัดออกจากตัวเลือกแล้วแต่สูตรราคายังอยู่เพื่อออเดอร์เก่า (2026-08-11 ตัด laser/portfolio/poster ออกจากตัวเลือก — โค้ดราคา/label เก่ายังอยู่เป็น dead branch เพื่อให้ออเดอร์เก่าแสดงได้) · `pro` = แคตตาล็อก `PRO_TYPES` 9 ประเภท (เพลท A0-A4, โปสเตอร์ A0-A4, **Postcard 9ใบ/แผ่น A3**, พับครึ่ง A2-A4 + `PRO_BINDS`, Portfolio, สติ๊กเกอร์ A3, นามบัตร, Standy, **แบบแปลนขาวดำ A1-A3**) — **ราคาจริงแล้ว** (2026-08-20 commit cc50eec): ค่าคงที่ PRO_SHEET_PRICE/PRO_BOARD_PRICE/PRO_LAM_*/PRO_POSTCARD_SHEET/PRO_STICKER_SHEET/PRO_PORTFOLIO/PRO_NAMECARD/PRO_STANDEE/BLUEPRINT_PRICE ต้นไฟล์ index.html จุดเดียว; แผ่นรวม = pages×copies; ตัวเลือกเสริม state proBoard/proSpecial/proLam/proStaple/proStand ถูกพ่วงเข้า `proLabel()` → คอลัมน์ `paper` หลังบ้านเห็นครบ; **ทุกประเภทมีเรทครบแล้ว** (foldbook ตัวสุดท้าย 2026-08-24: POSTER_SHEET หน้า-หลัง ×2 + PRO_FOLDBOOK_BIND 20/ชุดทุกแบบ + เคลือบ/กระดาษพิเศษ — ป้ายยืนยันทาง LINE ถูกถอดหมด); บันทึกเป็น `ptype:'pro'` · **สั่งได้ 24 ชม.** — หลัง 17:00 ถึงก่อน 08:30 (vals.proAfterCut) แจ้งเตือนเริ่มผลิตวันถัดไป 08:30 + ETA นับจาก 08:30 วันถัดไป (เดิมเคยบล็อก 08:30-15:00 — ยกเลิกแล้ว 2026-08-24)
- **ทิศทางกระดาษ + การกำหนดหน้า** (2026-08-19, เฉพาะ standard): state `orient` ('portrait'/'landscape') + `nup` (1/2/4/6 หน้า/แผ่น) → คอลัมน์ `orders.orient/nup`; โชว์ในหน้ารีวิว + สเปกการ์ด/CSV ของ admin; saveOrder ใช้ลูป optionalCols ตัดคอลัมน์ที่ฐานข้อมูลยังไม่มีแบบสะสม (2 รอบ) — เพิ่มคอลัมน์เสริมใหม่ให้เพิ่มใน array นี้ · **nup ลดราคาตามกระดาษจริง** (commit 252d5a8): ขาวดำ = หน้า/แผ่น = nup×(หน้า-หลัง?2:1) ปัดขึ้นรายไฟล์ แล้วเข้าเรทขั้นตามจำนวนแผ่นเหมือน duplex เดิม (×1.5 หน้า-หลังคงอยู่) · ปริ้นสี = ราคารวมหาร nup ปัดขึ้น · orient ไม่มีผลราคา
- ขั้นตอนสั่ง 4 สเต็ป: order → delivery → **review** (หน้าตรวจสอบสเปก+รูปตัวอย่างหน้าแรกของทุกไฟล์ ก่อนจ่าย) → pay; รูปตัวอย่าง (`files[].thumb` dataURL จาก `scanColor()` หน้าแรก 220px) อยู่ใน state เท่านั้น **ไม่บันทึกลง DB**
- ไฟล์แนบ: PDF เท่านั้น ≤50MB อัปเข้า storage / **>50MB ไม่ปฏิเสธ** แต่เก็บเป็น `files[].drive_url` (ลิงก์ Google Drive) — จำนวนหน้ายังนับได้จากไฟล์ในเครื่องลูกค้าด้วย pdf.js, ธง `files[].auto` บอกหลังบ้านว่าตัวเลขเชื่อได้ (auto) หรือลูกค้ากรอกเอง (ต้องตรวจก่อนปริ้น)
- ราคา: `CONFIG` + `PRINT_TYPES`/`bwSheetRate()`/`POSTER_PRICE` ต้นไฟล์ `index.html` — กลางคืน 22:00-06:00 +฿10 (`nightFee`), Express +฿20
- **ค่าส่ง 2 ชั้นตามระยะถนนจริง** (2026-08-14): จุดร้าน `CONFIG.shopLat/shopLng` = **โรงพิมพ์อุดมสุขปริ้นติ้ง กังสดาล 16.4633326,102.8241938** → OSRM (router.project-osrm.org, ล่มแล้ว fallback เส้นตรง×1.3 ใน `updateDistance()`) — **ลูกค้าเลือกพาหนะเอง** (state `vehicle` 'bike'/'car' ค่าเริ่มต้น bike — ปุ่มเลือกอยู่บนสุดของหน้าจุดส่ง; 2026-08-18 เลิกบังคับรถยนต์อัตโนมัติ `needsCar()`→`bigJob()` เหลือแค่ข้อความแนะนำสำหรับ Standy/เพลท A3+); พาหนะที่เลือกถูกนำหน้าคอลัมน์ `addr` เป็นอิโมจิ 🏍️/🚗 (ไม่มีคอลัมน์แยก): 🏍️ ไม่เกิน 2 กม. เหมา `deliveryBase` 19 แล้ว + `bikePerKm` 6 ×(ceil(กม.)−2) ตั้งแต่ กม.3 (แก้ 2026-08-24) **ไม่มีเพดานระยะ** · 🚗 `carTiers` ≤2=69 ≤4=89 ≤6=129 (ช่วง 4-6 มีป้ายนัดล่วงหน้า) เกิน 6 กม. บล็อกที่ toPay ให้สั่ง LINE; บรรทัด `distLine` ใต้แผนที่โชว์พาหนะ+กม.+ค่าส่งสด
- **ขาวดำคิดต่อ "หน้าที่พิมพ์จริง"** (นโยบาย 2026-09-05): ≤100 หน้า ฿1 · เกิน 100 หน้า ฿0.75 ตามหน้ารวมทั้งงาน (ทุกไฟล์ × จำนวนชุด); หน้า-หลังคิดทั้ง 2 ด้านเรทเดียวกัน (ไม่มี ×1.5 แล้ว); nup หารจำนวนหน้าที่คิดเงินตามจริง (ปัดขึ้นรายไฟล์) — `bwSheetRate()` + `calc()` ใน index.html
- **ปริ้นสีคิดตามปริมาณสีอัตโนมัติ**: `scanColor()` วาดแต่ละหน้าเป็นภาพย่อ ~140px (`intent:'print'` — ห้ามใช้ default intent, บางเบราว์เซอร์/แท็บพื้นหลังไม่ยิง rAF แล้วค้าง) วัดสัดส่วนพิกเซลที่เป็นหมึกสี; หน้าสีเยอะ ฿`colorHeavyPrice` (7) เมื่อ: สีสดเกิน `colorHeavyPct` (50%) ของหน้า **หรือ** เป็นหน้ารูปภาพเต็มหน้า (หมึกคลุม > `photoInkPct` 80% และสีจริง > `photoMinColorPct` 15% — กันภาพถ่ายโทนหม่นหลุดเกณฑ์), ที่เหลือ ฿`colorLightPrice` (5) — **แก้ราคา/เกณฑ์ที่ CONFIG จุดเดียว** บันทึกลง `files[].heavy` (จำนวนหน้าสีเยอะ) + `files[].scanned`; ไฟล์เกิน 400 หน้า/สแกนไม่ได้ = คิดเรทสีน้อยไว้ก่อน หลังบ้านขึ้นป้ายแดงให้ตรวจเอง
- โซนไรเดอร์ (admin): `zoneOf(lat,lng)` — กรอบรั้ว มข. = ในมอ, นอกนั้นวัดระยะหาจุดอ้างอิง กังสดาล/โคลัมโบ/หลังมอ (>3.5กม. = นอกเขต) — พิกัดเป็นค่าประมาณ ปรับได้ตามที่เจ้าของแจ้ง

## แบนเนอร์โปรโมชั่นแบบรูปภาพ (2026-08-19)

- เจ้าของออกแบบรูปเอง **1200×400 px (แนวนอน 3:1)** JPG/PNG แล้วอัปในแท็บ 📢 โปรโมชั่นของ admin.html → ขึ้น `uploads/banners/<ts>_<ชื่อไฟล์>` + คอลัมน์ `banners.image_path` → หน้าบ้านแสดง `<img>` ที่ `aspect-ratio:3/1` (ห้ามกลับไปใช้ height ตายตัว — รูปจะโดนครอป); ไม่ใส่รูป = การ์ดสี+อีโมจิแบบเดิม; มีรูปแล้วไม่ต้องพิมพ์ข้อความก็สร้างได้ (title ใช้เป็น alt เท่านั้น); ลบแบนเนอร์ = พยายามลบไฟล์รูปใน storage ด้วย (try/catch ไม่บล็อก)

## ประกาศแจ้งปัญหา/งานล่าช้า

- ใช้ตาราง `banners` เดิม: แถวที่ `color:'alert'` + active = ประกาศ (มีได้ทีละอัน — เปิดใหม่จะปิดอันเก่าอัตโนมัติ) ไม่ต้องมี SQL เพิ่ม; anon เขียน banners ไม่ได้ (ตรวจแล้ว 401) จึงปลอมประกาศไม่ได้
- ฝั่งลูกค้า: แบนเนอร์แดงในหน้าแรก + **ป๊อปอัพถามก่อนเข้าหน้าสั่ง** (`alertPrompt`/`alertAck` — ถามครั้งเดียวต่อการเปิดเว็บ ผ่านทั้งปุ่มเริ่มสั่งและเมนูล่าง) + แถบแดงบนหน้าสั่ง
- ฝั่งแอดมิน: แผงบนสุดของแท็บออเดอร์ — ปุ่ม 🚨 → ฟอร์ม (มี preset 2 แบบ + พิมพ์เอง) → เปิดประกาศ / ปิดประกาศ (set active false) / ลบทิ้ง

## หน้า "เกี่ยวกับเรา" (แท็บล่างขวา แทนแท็บ LINE เดิม)

- screen `'about'` (2026-08-17): การ์ดข้อมูลร้าน (โลโก้ + สโลแกน + ⭐ เรตติ้งจาก `revAvg`/`revCount` + ชิป 24ชม./ส่งถึงหอ/มารับเอง) + ปุ่มช่องทาง `aboutLinks` ใน vals: LINE OA (`MP_CONFIG.LINE_OA_URL`), TikTok/Facebook/Instagram @moreprintservice — โลโก้แต่ละแอพวาดเป็น **SVG ฝังในไฟล์** (ห้ามเปลี่ยนเป็นรูปโหลดนอก — กันลิงก์เสีย/CSP) แก้ลิงก์ที่ array เดียว

## ระบบรีวิว

- ตาราง `reviews` (order_id/name/stars/comment/ptype/auto): anon SELECT+INSERT เท่านั้น, admin ลบได้ผ่าน `is_admin()` — ฟอร์มหน้าแรก + ฟอร์มหน้าติดตามเมื่อสถานะ = ส่งแล้ว
- **5 ดาวอัตโนมัติ**: เมื่อเห็นสถานะ 4 ครั้งแรก (`maybeAutoReview` กันซ้ำด้วย localStorage `mp_review_<id>`) แถวใหม่ auto:true; ถ้าลูกค้ารีวิวเองทีหลัง insert แถวใหม่ (auto:false) — จอแสดงผล dedupe ต่อ order_id เอาแถวใหม่สุด (ห้ามให้ anon มีสิทธิ์ UPDATE)
- UI รีวิวทั้งหมดซ่อนจนกว่า `loadReviews()` สำเร็จ (`reviewsReady`) — ถ้าตารางยังไม่ถูกสร้าง เว็บทำงานปกติไม่มี error; `mp_order.plabel` เก็บป้ายประเภทงานไว้แสดงในรีวิว

## ข้อจำกัดแวดล้อมที่เจอมาแล้ว (อย่าเสียเวลาซ้ำ)

- **ลิงก์แชร์ "ร้าน/สถานที่" ของ Google Maps ไม่มีพิกัดแล้ว** (ส.ค. 2026): กางออกเป็น `maps?q=<ชื่อ+ที่อยู่>&ftid=0x...` — พิกัดไม่อยู่ทั้งใน URL และ HTML ฝั่ง server (ลองมาแล้วทุก UA รวม facebookexternalhit) และ geocoder ฟรีหาร้านไทยไม่เจอ ห้ามเสียเวลาแกะอีก; ทางแก้ที่ใช้อยู่: useGmapLink เด้งไปโหมดปักหมุด + ค้นชื่อจาก q= อัตโนมัติ; ลิงก์แชร์แบบปักหมุดเปล่า/ตำแหน่งปัจจุบันยังมีพิกัดปกติ
- **หลังรัน ALTER TABLE orders**: RPC/REST อาจรวนชั่วครู่ (cached plan บน connection pool) — อาการ: ยกเลิก/อ่านสถานะล้มเหลวแบบสุ่มแล้วหายเอง; ให้ทดสอบซ้ำก่อนไล่หาสาเหตุอื่น

- **แก้ schema/SQL ของ Supabase**: dashboard เปิดใน Chrome ของเจ้าของแล้วจอขาว (สาเหตุยังหาไม่เจอ แม้ลบ adblock แล้ว) → ให้เจ้าของรัน SQL ใน **Safari** แทน (ทำเป็นประจำอยู่แล้ว ส่ง SQL ก้อนเดียวให้วางแล้วกด Run)
- ฟอร์มของ LINE Developers console และ Supabase dashboard **ปฏิเสธ synthetic input** (คลิก/พิมพ์อัตโนมัติไม่ติด) — งานพวกนั้นต้องให้เจ้าของกดเอง พร้อมบอกค่าที่ต้องกรอกทีละช่อง
- `liff.login()` ต้องใช้ redirect default เท่านั้น (custom redirectUri ที่ไม่ตรง endpoint เป๊ะ = 400); เปิดจากเบราว์เซอร์ในแอป LINE จะ redirect ไป `https://liff.line.me/<LIFF_ID>` อัตโนมัติ
- GitHub Actions cron หยุดเองถ้า repo ไม่มี commit 60 วัน (มีเมลเตือนให้กดเปิดใหม่)

## บัญชี/ของทดสอบที่มีอยู่

- บัญชีสมาชิกทดสอบ (ฝั่งลูกค้า): `test-member-mp@gmail.com`
- ออเดอร์ทดสอบใช้ชื่อลูกค้าขึ้นต้นว่า "ทดสอบ…" เสมอ — เจ้าของลบทิ้งได้ทุกเมื่อ
